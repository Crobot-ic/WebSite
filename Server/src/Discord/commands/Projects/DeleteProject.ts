import { Client, CommandInteraction } from "discord.js";

const deleteProject = (client: Client, interaction: CommandInteraction) => {
    // Fetch the message ID and the image localization where the project is displayed
    // Check if the project exists
    // Delete this message 
    // Delete from DB
    // Delete the image on GoogleDrive
}

export default deleteProject;